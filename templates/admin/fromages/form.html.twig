{% extends 'base.html.twig' %}

{% block title %}
    {{ fromage is defined ? 'Modifier le fromage' : 'Créer un fromage' }}
{% endblock %}

{% block body %}
<h1>{{ fromage is defined ? 'Modifier le fromage' : 'Créer un fromage' }}</h1>

{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="flash-{{ label }}">
            {{ message }}
        </div>
    {% endfor %}
{% endfor %}

{{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}
        <div>
            {{ form_label(form.titreFromage) }}
            {{ form_widget(form.titreFromage) }}
            {{ form_errors(form.titreFromage) }}
        </div>
        <div>
            {{ form_label(form.category) }}
            {{ form_widget(form.category) }}
            {{ form_errors(form.category) }}
        </div>
        <div>
            {{ form_label(form.description) }}
            {{ form_widget(form.description) }}
            {{ form_errors(form.description) }}
        </div>
        <div>
            {{ form_label(form.stock) }}
            {{ form_widget(form.stock) }}
            {{ form_errors(form.stock) }}
        </div>
        <div>
            {{ form_label(form.minCommande) }}
            {{ form_widget(form.minCommande) }}
            {{ form_errors(form.minCommande) }}
        </div>
        <div>
            {{ form_label(form.prixParFromage) }}
            {{ form_widget(form.prixParFromage) }}
            {{ form_errors(form.prixParFromage) }}
        </div>
        <div>
            {{ form_label(form.image) }}
            {{ form_widget(form.image) }}
            {{ form_errors(form.image) }}
        </div>
        <div>
            {{ form_label(form.altText) }}
            {{ form_widget(form.altText) }}
            {{ form_errors(form.altText) }}
        </div>

        <button type="submit" class="btn">
            {% if plat is defined %}
                <img src="{{ asset('Visuels/Icônes/Edit_Beige.png') }}" alt="Modifier">Modifier le fromage
            {% else %}
                <img src="{{ asset('Visuels/Icônes/Plus_Beige.png') }}" alt="Créer">Créer le fromage
            {% endif %}
        </button>
{{ form_end(form) }}

<a href="{{ path('fromages_list') }}" class="btn">
    <img src="{{ asset('Visuels/Icônes/Back_Beige.png') }}" alt="Retour">Retour à la liste
</a>

{% endblock %}